{
  "name": "ump-cferi-web",
  "version": "1.0.0",
  "description": "UMP CFERI Platform",
  "license": "MIT",
  "scripts": {
    "start": "cd backend && npm start",
    "build": "cd frontend && npm run build",
    "dev": "concurrently \"cd backend && npm start\" \"cd frontend && npm start\""
  },
  "devDependencies": {
    "concurrently": "^8.0.1"
  },
  "dependencies": {
    "dotenv": "^17.3.1",
    "mongodb": "^7.1.0"
  }
}
